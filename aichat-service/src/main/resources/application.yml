dashscope:
  api:
    key: # 输入你自己的DashScope API Key
server:
  port: 8081

spring:
  mvc:
    cors:
      enabled: false
  cloud:
    nacos:
      discovery:
        server-addr: 127.0.0.1:8848
        username: nacos
        password: nacos
      config:
        username: nacos  # 需要同时配置config的认证
        password: nacos
  application:
    name: aichat-service